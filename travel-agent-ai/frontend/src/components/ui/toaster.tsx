import { useToast } from "@/components/ui/use-toast";
export function Toaster() {
  // Minimal no-op toaster to satisfy import; integrate your actual toast UI as needed
  useToast();
  return null;
}
export default Toaster;
